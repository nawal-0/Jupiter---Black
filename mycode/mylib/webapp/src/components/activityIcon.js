import React from "react";

function ActivityIcon({image, data}) {
    return (
        <div>
            <img src={image} alt="activity icon" />
          
        </div>
    );
}

export default ActivityIcon;